import { Users } from "lucide-react";
import { PlaceholderPage } from "@/features/_placeholder/PlaceholderPage";

export function PartnersPage() {
  return (
    <PlaceholderPage
      title="Partners"
      comingInSession={14}
      Icon={Users}
      description="Partner roster, follow-up cadences, partner-sourced lead attribution, and the Partner Portal access toggle."
    />
  );
}
